# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['windowff.py'],
    pathex=[],
    binaries=[],
    datas=[('background.mp3', '.'), ('background2.mp3', '.'), ('background3.mp3', '.'), ('startup.wav', '.'), ('snttxt.wav', '.'), ('type.wav', '.'), ('icon.ico', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='windowff',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['icon.ico'],
)
